# 开发阶段测试文档 - 企业员工管理系统

## 1. 测试概述

### 1.1 测试基本信息

| 项目 | 说明 |
|------|------|
| 项目名称 | 企业员工管理系统 |
| 测试类型 | 单元测试、接口测试、集成测试 |
| 测试阶段 | 开发阶段 |
| 测试人 | 张三（单人开发） |
| 测试日期 | 2026-08-25 |
| 测试版本 | V1.0 |

### 1.2 测试目的
本次测试旨在验证企业员工管理系统各模块功能的正确性、稳定性和安全性，确保开发质量，为系统交付提供质量保障。

### 1.3 测试范围
- 单元测试：Service层核心业务逻辑
- 接口测试：RESTful API接口
- 集成测试：跨模块功能验证

### 1.4 测试环境

| 环境项 | 配置 |
|--------|------|
| 操作系统 | Windows 11 / Ubuntu 20.04 |
| JDK | Java 17 |
| Node.js | 18.x |
| 数据库 | MySQL 8.0 |
| 缓存 | Redis 7.x |
| 测试框架 | JUnit 5、Mockito、Postman/Newman |
| 构建工具 | Maven、npm |

---

## 2. 单元测试

### 2.1 测试规范

| 规范项 | 要求 |
|--------|------|
| 测试覆盖率 | 核心业务逻辑≥80% |
| 测试方法 | BDD风格（Given-When-Then） |
| 测试命名 | test_{方法名}_{场景}_{预期结果} |
| 断言 | 必须使用断言验证结果 |
| Mock | 外部依赖使用Mock模拟 |

### 2.2 认证模块测试

#### 测试用例清单

| 用例编号 | 测试方法 | 测试场景 | 优先级 | 状态 |
|----------|----------|----------|--------|------|
| AUTH-001 | test_login_success | 正确用户名密码登录 | 高 | 待执行 |
| AUTH-002 | test_login_wrong_password | 错误密码登录 | 高 | 待执行 |
| AUTH-003 | test_login_user_not_found | 用户不存在登录 | 高 | 待执行 |
| AUTH-004 | test_login_user_disabled | 禁用用户登录 | 高 | 待执行 |
| AUTH-005 | test_login_account_locked | 锁定账号登录 | 高 | 待执行 |
| AUTH-006 | test_refresh_token_success | 刷新Token成功 | 中 | 待执行 |
| AUTH-007 | test_refresh_token_expired | 过期Token刷新 | 中 | 待执行 |
| AUTH-008 | test_logout_success | 登出成功 | 中 | 待执行 |

#### 测试用例详情

**AUTH-001：正确用户名密码登录**
```java
@Test
@DisplayName("正确用户名密码登录成功")
void test_login_success() {
    // Given
    String username = "admin";
    String password = "correctPassword";
    User mockUser = User.builder()
        .username(username)
        .password(passwordEncoder.encode(password))
        .status(1)
        .build();
    
    when(userRepository.findByUsername(username)).thenReturn(mockUser);
    when(passwordEncoder.matches(password, mockUser.getPassword())).thenReturn(true);
    
    // When
    LoginResult result = authService.login(username, password);
    
    // Then
    assertNotNull(result);
    assertNotNull(result.getToken());
    assertEquals(24, result.getExpiresIn());
}
```

**AUTH-002：错误密码登录**
```java
@Test
@DisplayName("错误密码登录抛出异常")
void test_login_wrong_password() {
    // Given
    String username = "admin";
    String password = "wrongPassword";
    User mockUser = User.builder()
        .username(username)
        .password(passwordEncoder.encode("correctPassword"))
        .status(1)
        .build();
    
    when(userRepository.findByUsername(username)).thenReturn(mockUser);
    when(passwordEncoder.matches(password, mockUser.getPassword())).thenReturn(false);
    
    // When & Then
    assertThrows(BusinessException.class, () -> {
        authService.login(username, password);
    });
    
    verify(userRepository).findByUsername(username);
}
```

### 2.3 员工管理模块测试

#### 测试用例清单

| 用例编号 | 测试方法 | 测试场景 | 优先级 | 状态 |
|----------|----------|----------|--------|------|
| EMP-001 | test_create_employee_success | 创建员工成功 | 高 | 待执行 |
| EMP-002 | test_create_employee_duplicate_no | 工号重复创建 | 高 | 待执行 |
| EMP-003 | test_get_employee_by_id | 根据ID查询员工 | 高 | 待执行 |
| EMP-004 | test_get_employee_list | 查询员工列表分页 | 高 | 待执行 |
| EMP-005 | test_update_employee_success | 更新员工信息 | 高 | 待执行 |
| EMP-006 | test_delete_employee_success | 逻辑删除员工 | 高 | 待执行 |
| EMP-007 | test_resign_employee_success | 办理离职手续 | 高 | 待执行 |
| EMP-008 | test_search_employee_by_keyword | 关键词搜索员工 | 中 | 待执行 |

#### 测试用例详情

**EMP-001：创建员工成功**
```java
@Test
@DisplayName("创建员工成功")
void test_create_employee_success() {
    // Given
    EmployeeCreateDTO dto = EmployeeCreateDTO.builder()
        .empNo("EMP202608001")
        .name("张三")
        .gender(1)
        .deptId(2L)
        .entryDate(LocalDate.now())
        .build();
    
    Employee savedEmployee = Employee.builder()
        .id(1L)
        .empNo(dto.getEmpNo())
        .name(dto.getName())
        .build();
    
    when(employeeRepository.save(any(Employee.class))).thenReturn(savedEmployee);
    
    // When
    EmployeeVO result = employeeService.createEmployee(dto);
    
    // Then
    assertNotNull(result);
    assertEquals(1L, result.getId());
    assertEquals("EMP202608001", result.getEmpNo());
    assertEquals("张三", result.getName());
    verify(employeeRepository).save(any(Employee.class));
}
```

**EMP-002：工号重复创建**
```java
@Test
@DisplayName("工号重复创建抛出异常")
void test_create_employee_duplicate_no() {
    // Given
    EmployeeCreateDTO dto = EmployeeCreateDTO.builder()
        .empNo("EMP202608001")
        .name("张三")
        .build();
    
    when(employeeRepository.existsByEmpNo(dto.getEmpNo())).thenReturn(true);
    
    // When & Then
    assertThrows(BusinessException.class, () -> {
        employeeService.createEmployee(dto);
    });
    
    verify(employeeRepository).existsByEmpNo(dto.getEmpNo());
    verify(employeeRepository, never()).save(any(Employee.class));
}
```

### 2.4 考勤管理模块测试

#### 测试用例清单

| 用例编号 | 测试方法 | 测试场景 | 优先级 | 状态 |
|----------|----------|----------|--------|------|
| ATT-001 | test_check_in_success | 上班打卡成功 | 高 | 待执行 |
| ATT-002 | test_check_out_success | 下班打卡成功 | 高 | 待执行 |
| ATT-003 | test_check_in_already_checked | 重复打卡异常 | 高 | 待执行 |
| ATT-004 | test_apply_leave_success | 请假申请成功 | 高 | 待执行 |
| ATT-005 | test_approve_leave_success | 请假审批成功 | 高 | 待执行 |
| ATT-006 | test_approve_leave_reject | 请假审批驳回 | 高 | 待执行 |
| ATT-007 | test_monthly_attendance_stats | 月度考勤统计 | 中 | 待执行 |

#### 测试用例详情

**ATT-001：上班打卡成功**
```java
@Test
@DisplayName("上班打卡成功")
void test_check_in_success() {
    // Given
    Long empId = 1L;
    CheckInDTO dto = CheckInDTO.builder()
        .checkType("IN")
        .build();
    
    Employee employee = Employee.builder()
        .id(empId)
        .name("张三")
        .build();
    
    Attendance attendance = Attendance.builder()
        .id(1L)
        .empId(empId)
        .attendanceDate(LocalDate.now())
        .checkInTime(LocalDateTime.now())
        .workStatus(1)
        .build();
    
    when(employeeRepository.findById(empId)).thenReturn(Optional.of(employee));
    when(attendanceRepository.save(any(Attendance.class))).thenReturn(attendance);
    
    // When
    AttendanceVO result = attendanceService.checkIn(empId, dto);
    
    // Then
    assertNotNull(result);
    assertEquals(1, result.getWorkStatus());
    assertNotNull(result.getCheckInTime());
}
```

### 2.5 薪资管理模块测试

#### 测试用例清单

| 用例编号 | 测试方法 | 测试场景 | 优先级 | 状态 |
|----------|----------|----------|--------|------|
| SAL-001 | test_calculate_salary | 薪资计算正确 | 高 | 待执行 |
| SAL-002 | test_calculate_salary_with_leave | 有请假扣款计算 | 高 | 待执行 |
| SAL-003 | test_calculate_salary_with_overtime | 有加班费计算 | 高 | 待执行 |
| SAL-004 | test_generate_monthly_salary | 月度薪资生成 | 高 | 待执行 |
| SAL-005 | test_approve_salary_success | 薪资审批通过 | 高 | 待执行 |

#### 测试用例详情

**SAL-001：薪资计算正确**
```java
@Test
@DisplayName("基础薪资计算正确")
void test_calculate_salary() {
    // Given
    SalaryPlan plan = SalaryPlan.builder()
        .baseSalary(15000.00)
        .performanceSalary(5000.00)
        .postAllowance(2000.00)
        .mealAllowance(500.00)
        .build();
    
    int workDays = 22;
    int leaveDays = 0;
    double overtimeHours = 0;
    
    // When
    SalaryCalculateResult result = salaryCalculator.calculate(plan, workDays, leaveDays, overtimeHours);
    
    // Then
    assertEquals(22500.00, result.getGrossSalary(), 0.01);
    assertEquals(22500.00, result.getNetSalary(), 0.01); // 简化计算
}
```

**SAL-002：有请假扣款计算**
```java
@Test
@DisplayName("有请假扣款的薪资计算")
void test_calculate_salary_with_leave() {
    // Given
    SalaryPlan plan = SalaryPlan.builder()
        .baseSalary(15000.00)
        .performanceSalary(5000.00)
        .build();
    
    int workDays = 22;
    int leaveDays = 2;
    
    // When
    SalaryCalculateResult result = salaryCalculator.calculate(plan, workDays, leaveDays, 0);
    
    // Then
    double expectedLeaveDeduction = (15000.00 + 5000.00) / 21.75 * 2;
    assertEquals(expectedLeaveDeduction, result.getLeaveDeduction(), 0.01);
}
```

---

## 3. 接口测试

### 3.1 测试工具
- Postman：接口调试
- Newman：命令行执行Postman集合
- curl：命令行工具

### 3.2 接口测试用例

#### 认证接口测试

| 用例编号 | 接口 | 方法 | 测试场景 | 预期状态码 | 预期结果 |
|----------|------|------|----------|------------|----------|
| API-AUTH-001 | /api/v1/auth/login | POST | 正确登录 | 200 | 返回Token |
| API-AUTH-002 | /api/v1/auth/login | POST | 错误密码 | 401 | 返回错误信息 |
| API-AUTH-003 | /api/v1/auth/login | POST | 用户不存在 | 401 | 返回错误信息 |
| API-AUTH-004 | /api/v1/auth/logout | POST | 正常登出 | 200 | 登出成功 |
| API-AUTH-005 | /api/v1/auth/refresh-token | POST | 刷新Token | 200 | 返回新Token |
| API-AUTH-006 | /api/v1/auth/current-user | GET | 获取当前用户 | 200 | 返回用户信息 |

**Postman测试集合示例（登录接口）：**

```json
{
  "info": {
    "name": "企业员工管理系统API测试",
    "_postman_id": "test-collection-id"
  },
  "item": [
    {
      "name": "用户登录-成功",
      "request": {
        "method": "POST",
        "header": [{"key": "Content-Type", "value": "application/json"}],
        "body": {
          "mode": "raw",
          "raw": "{\"username\":\"admin\",\"password\":\"admin123\"}"
        },
        "url": "{{baseUrl}}/api/v1/auth/login"
      },
      "response": {
        "status": "OK",
        "code": 200,
        "header": [{"key": "Content-Type", "value": "application/json"}],
        "body": "{\n  \"code\": 200,\n  \"message\": \"登录成功\",\n  \"data\": {\n    \"token\": \"eyJhbGciOiJIUzI1NiJ9...\",\n    \"expiresIn\": 86400\n  }\n}"
      }
    },
    {
      "name": "用户登录-密码错误",
      "request": {
        "method": "POST",
        "header": [{"key": "Content-Type", "value": "application/json"}],
        "body": {
          "mode": "raw",
          "raw": "{\"username\":\"admin\",\"password\":\"wrongpassword\"}"
        },
        "url": "{{baseUrl}}/api/v1/auth/login"
      },
      "response": {
        "status": "Unauthorized",
        "code": 401,
        "header": [{"key": "Content-Type", "value": "application/json"}],
        "body": "{\n  \"code\": 2001,\n  \"message\": \"用户名或密码错误\",\n  \"data\": null\n}"
      }
    }
  ],
  "variable": [
    {"key": "baseUrl", "value": "http://localhost:8080"}
  ]
}
```

#### 员工管理接口测试

| 用例编号 | 接口 | 方法 | 测试场景 | 预期状态码 | 预期结果 |
|----------|------|------|----------|------------|----------|
| API-EMP-001 | /api/v1/employees | GET | 查询员工列表 | 200 | 返回分页列表 |
| API-EMP-002 | /api/v1/employees/{id} | GET | 查询员工详情 | 200 | 返回员工详情 |
| API-EMP-003 | /api/v1/employees | POST | 创建员工 | 201 | 返回新员工ID |
| API-EMP-004 | /api/v1/employees/{id} | PUT | 更新员工 | 200 | 更新成功 |
| API-EMP-005 | /api/v1/employees/{id} | DELETE | 删除员工 | 200 | 删除成功 |
| API-EMP-006 | /api/v1/employees/{id}/resign | POST | 离职登记 | 200 | 登记成功 |
| API-EMP-007 | /api/v1/employees/export | GET | 导出员工 | 200 | 返回Excel文件 |

#### 部门管理接口测试

| 用例编号 | 接口 | 方法 | 测试场景 | 预期状态码 | 预期结果 |
|----------|------|------|----------|------------|----------|
| API-DEPT-001 | /api/v1/departments/tree | GET | 查询部门树 | 200 | 返回树形结构 |
| API-DEPT-002 | /api/v1/departments | POST | 创建部门 | 201 | 创建成功 |
| API-DEPT-003 | /api/v1/departments/{id} | PUT | 更新部门 | 200 | 更新成功 |
| API-DEPT-004 | /api/v1/departments/{id} | DELETE | 删除部门 | 200 | 删除成功 |
| API-DEPT-005 | /api/v1/departments/{id} | DELETE | 删除有员工的部门 | 400 | 返回错误信息 |

#### 考勤管理接口测试

| 用例编号 | 接口 | 方法 | 测试场景 | 预期状态码 | 预期结果 |
|----------|------|------|----------|------------|----------|
| API-ATT-001 | /api/v1/attendance/check-in | POST | 上班打卡 | 200 | 打卡成功 |
| API-ATT-002 | /api/v1/attendance | GET | 查询考勤记录 | 200 | 返回考勤列表 |
| API-ATT-003 | /api/v1/attendance/statistics | GET | 月度考勤统计 | 200 | 返回统计数据 |
| API-ATT-004 | /api/v1/attendance/leaves | POST | 提交请假 | 201 | 申请成功 |
| API-ATT-005 | /api/v1/attendance/leaves/{id}/approve | POST | 审批请假 | 200 | 审批成功 |

#### 薪资管理接口测试

| 用例编号 | 接口 | 方法 | 测试场景 | 预期状态码 | 预期结果 |
|----------|------|------|----------|------------|----------|
| API-SAL-001 | /api/v1/salary/plans | GET | 查询薪资方案 | 200 | 返回方案列表 |
| API-SAL-002 | /api/v1/salary/records | GET | 查询薪资记录 | 200 | 返回薪资列表 |
| API-SAL-003 | /api/v1/salary/records/generate | POST | 生成月度薪资 | 200 | 生成成功 |
| API-SAL-004 | /api/v1/salary/records/{id}/approve | POST | 审批薪资 | 200 | 审批成功 |

#### 系统管理接口测试

| 用例编号 | 接口 | 方法 | 测试场景 | 预期状态码 | 预期结果 |
|----------|------|------|----------|------------|----------|
| API-SYS-001 | /api/v1/system/users | GET | 查询用户列表 | 200 | 返回用户列表 |
| API-SYS-002 | /api/v1/system/users | POST | 创建用户 | 201 | 创建成功 |
| API-SYS-003 | /api/v1/system/roles | GET | 查询角色列表 | 200 | 返回角色列表 |
| API-SYS-004 | /api/v1/system/roles | POST | 创建角色 | 201 | 创建成功 |
| API-SYS-005 | /api/v1/system/operate-logs | GET | 查询操作日志 | 200 | 返回日志列表 |

#### 统计分析接口测试

| 用例编号 | 接口 | 方法 | 测试场景 | 预期状态码 | 预期结果 |
|----------|------|------|----------|------------|----------|
| API-STAT-001 | /api/v1/statistics/dashboard/employee | GET | 员工数据看板 | 200 | 返回统计数据 |
| API-STAT-002 | /api/v1/statistics/dashboard/attendance | GET | 考勤数据看板 | 200 | 返回统计数据 |
| API-STAT-003 | /api/v1/statistics/dashboard/salary | GET | 薪资数据看板 | 200 | 返回统计数据 |

---

## 4. 集成测试

### 4.1 测试场景

#### 场景1：新员工入职流程

| 步骤 | 操作 | 接口 | 预期结果 | 状态 |
|------|------|------|----------|------|
| 1 | 创建用户账号 | POST /api/v1/system/users | 返回用户ID | 待执行 |
| 2 | 创建员工信息 | POST /api/v1/employees | 返回员工ID | 待执行 |
| 3 | 分配薪资方案 | PUT /api/v1/employees/{id}/salary-plan | 分配成功 | 待执行 |
| 4 | 验证登录 | POST /api/v1/auth/login | 登录成功 | 待执行 |
| 5 | 查看个人信息 | GET /api/v1/auth/current-user | 信息正确 | 待执行 |

#### 场景2：月度薪资结算流程

| 步骤 | 操作 | 接口 | 预期结果 | 状态 |
|------|------|------|----------|------|
| 1 | 统计月度考勤 | GET /api/v1/attendance/statistics | 返回考勤统计 | 待执行 |
| 2 | 生成月度薪资 | POST /api/v1/salary/records/generate | 返回生成数量 | 待执行 |
| 3 | 查看薪资记录 | GET /api/v1/salary/records | 返回薪资列表 | 待执行 |
| 4 | 审批薪资记录 | POST /api/v1/salary/records/{id}/approve | 审批成功 | 待执行 |
| 5 | 确认薪资发放 | POST /api/v1/salary/records/{id}/pay | 发放成功 | 待执行 |

#### 场景3：请假审批流程

| 步骤 | 操作 | 接口 | 预期结果 | 状态 |
|------|------|------|----------|------|
| 1 | 员工提交请假 | POST /api/v1/attendance/leaves | 返回请假单号 | 待执行 |
| 2 | 管理员查看待审批 | GET /api/v1/attendance/leaves?status=0 | 返回待审批列表 | 待执行 |
| 3 | 审批通过 | POST /api/v1/attendance/leaves/{id}/approve | 审批成功 | 待执行 |
| 4 | 查看考勤记录 | GET /api/v1/attendance?date=xxx | 请假标记正确 | 待执行 |

#### 场景4：员工离职流程

| 步骤 | 操作 | 接口 | 预期结果 | 状态 |
|------|------|------|----------|------|
| 1 | 提交离职申请 | POST /api/v1/employees/{id}/resign | 离职登记成功 | 待执行 |
| 2 | 停用用户账号 | PUT /api/v1/system/users/{id}/status | 账号禁用 | 待执行 |
| 3 | 验证无法登录 | POST /api/v1/auth/login | 登录失败 | 待执行 |
| 4 | 员工状态变更 | GET /api/v1/employees/{id} | 状态为离职 | 待执行 |

---

## 5. 性能测试

### 5.1 测试工具
- JMeter：Apache JMeter性能测试工具

### 5.2 测试场景

#### 场景1：登录接口并发测试

| 配置项 | 值 |
|--------|------|
| 线程数 | 50 |
| 爬坡时间 | 10秒 |
| 循环次数 | 10 |
| 持续时间 | 60秒 |

**性能指标要求：**
| 指标 | 要求 |
|------|------|
| 平均响应时间 | ≤500ms |
| 95%响应时间 | ≤1000ms |
| 错误率 | ≤1% |
| TPS | ≥50 |

#### 场景2：员工列表查询压力测试

| 配置项 | 值 |
|--------|------|
| 线程数 | 100 |
| 爬坡时间 | 20秒 |
| 循环次数 | 5 |
| 持续时间 | 120秒 |

**性能指标要求：**
| 指标 | 要求 |
|------|------|
| 平均响应时间 | ≤1000ms |
| 95%响应时间 | ≤3000ms |
| 错误率 | ≤1% |
| TPS | ≥100 |

#### 场景3：数据看板查询测试

| 配置项 | 值 |
|--------|------|
| 线程数 | 50 |
| 爬坡时间 | 10秒 |
| 循环次数 | 20 |
| 持续时间 | 120秒 |

**性能指标要求：**
| 指标 | 要求 |
|------|------|
| 平均响应时间 | ≤2000ms |
| 95%响应时间 | ≤5000ms |
| 错误率 | ≤1% |

### 5.3 测试结果记录

| 测试场景 | 线程数 | 平均响应时间 | 95%响应时间 | TPS | 错误率 | 是否达标 |
|----------|--------|--------------|--------------|-----|--------|----------|
| 登录接口 | 50 | - | - | - | - | 待测试 |
| 员工列表查询 | 100 | - | - | - | - | 待测试 |
| 数据看板 | 50 | - | - | - | - | 待测试 |

---

## 6. 安全测试

### 6.1 测试项目

| 测试项 | 测试方法 | 预期结果 | 状态 |
|--------|----------|----------|------|
| SQL注入测试 | 在参数中注入SQL语句 | 系统正常，无数据泄露 | 待测试 |
| XSS攻击测试 | 在输入框注入JavaScript代码 | 代码被转义，不执行 | 待测试 |
| 越权访问测试 | 普通用户访问管理员接口 | 返回403无权限 | 待测试 |
| Token过期测试 | 使用过期Token访问接口 | 返回401需重新登录 | 待测试 |
| 暴力破解测试 | 短时间内大量错误登录 | 账号被锁定 | 待测试 |
| 敏感数据泄露测试 | 检查响应中是否有敏感数据 | 身份证、手机号脱敏 | 待测试 |

### 6.2 SQL注入测试示例

**测试用例：登录接口**
```
用户名参数：
admin' OR '1'='1

预期结果：
1. 系统正常返回"用户名或密码错误"
2. 数据库无异常查询
3. 日志记录异常尝试
```

**测试用例：查询接口**
```
搜索参数：
' OR 1=1 --

预期结果：
1. 返回正常查询结果
2. 不返回全部数据
3. 参数被正确转义
```

### 6.3 越权访问测试示例

**测试用例：普通用户访问员工列表**
```
1. 使用普通员工Token请求 /api/v1/employees
2. 检查返回数据是否只包含本人信息
3. 检查是否返回其他员工敏感数据

预期结果：
1. 只能查看本人信息
2. 无其他员工数据泄露
3. 管理员可查看全部数据
```

---

## 7. 缺陷管理

### 7.1 缺陷等级定义

| 等级 | 定义 | 处理时间要求 |
|------|------|--------------|
| 致命（P0） | 系统崩溃、数据丢失、安全漏洞 | 24小时内修复 |
| 严重（P1） | 核心功能不可用、严重影响使用 | 48小时内修复 |
| 一般（P2） | 功能异常但有替代方案 | 3个工作日内修复 |
| 轻微（P3） | UI问题、提示信息不准确 | 下个迭代修复 |

### 7.2 缺陷记录模板

| 字段 | 说明 |
|------|------|
| 缺陷编号 | BUG-YYYYMMDD-NNN |
| 缺陷标题 | 简要描述问题 |
| 所属模块 | 功能模块名称 |
| 严重等级 | P0/P1/P2/P3 |
| 复现步骤 | 详细复现步骤 |
| 预期结果 | 正确预期结果 |
| 实际结果 | 实际发生的问题 |
| 提交时间 | 缺陷提交时间 |
| 处理状态 | 待处理/处理中/已修复/已验证/已关闭 |

### 7.3 缺陷统计（示例）

| 所属模块 | P0 | P1 | P2 | P3 | 合计 | 状态 |
|----------|----|----|----|----|------|------|
| 认证模块 | 0 | 0 | 2 | 3 | 5 | 待测试 |
| 员工管理 | 0 | 1 | 3 | 5 | 9 | 待测试 |
| 部门管理 | 0 | 0 | 1 | 2 | 3 | 待测试 |
| 考勤管理 | 0 | 1 | 4 | 6 | 11 | 待测试 |
| 薪资管理 | 0 | 2 | 3 | 4 | 9 | 待测试 |
| 绩效管理 | 0 | 0 | 2 | 3 | 5 | 待测试 |
| 系统管理 | 0 | 0 | 1 | 2 | 3 | 待测试 |
| 统计分析 | 0 | 0 | 2 | 4 | 6 | 待测试 |
| **合计** | **0** | **4** | **18** | **29** | **51** | - |

---

## 附录

### 附录A：测试数据准备

| 数据类型 | 数据描述 | 数量 | 用途 |
|----------|----------|------|------|
| 测试账号 | 管理员、HR、部门经理、普通员工 | 4 | 权限测试 |
| 测试部门 | 多级部门结构 | 10+ | 部门管理测试 |
| 测试员工 | 不同岗位、状态的员工 | 100+ | 员工管理测试 |
| 考勤数据 | 不同考勤状态记录 | 500+ | 考勤测试 |
| 薪资数据 | 不同薪资方案记录 | 100+ | 薪资测试 |

### 附录B：测试命令示例

```bash
# 运行单元测试
mvn test

# 运行指定测试类
mvn test -Dtest=AuthServiceTest

# 运行Postman集合
newman run test-collection.json -e environment.json

# 运行JMeter测试
jmeter -n -t performance-test.jmx -l result.jtl

# 生成测试报告
mvn surefire-report:report
```

---

**文档版本历史：**

| 版本 | 日期 | 修改人 | 修改内容 |
|------|------|--------|----------|
| V1.0 | 2026-08-25 | 张三（单人） | 初始版本，创建开发阶段测试文档 |
