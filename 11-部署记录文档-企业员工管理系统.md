# 部署记录文档 - 企业员工管理系统

## 1. 部署概述

### 1.1 部署基本信息

| 项目 | 说明 |
|------|------|
| 项目名称 | 企业员工管理系统 |
| 部署类型 | 生产环境部署 |
| 部署日期 | 2026-08-25 |
| 部署人 | 张三（单人开发） |
| 部署版本 | V1.0.0 |
| 部署方式 | Docker Compose容器化部署 |

### 1.2 部署环境

#### 硬件环境

| 项目 | 配置要求 | 实际配置 |
|------|----------|----------|
| 服务器 | 4核8G+ | 4核16G |
| 操作系统 | Ubuntu 20.04+ / CentOS 7+ | Ubuntu 22.04 |
| 系统盘 | ≥50GB SSD | 100GB SSD |
| 内存 | ≥8GB | 16GB |
| 带宽 | ≥5Mbps | 100Mbps |

#### 软件环境

| 软件 | 版本要求 | 实际版本 | 用途 |
|------|----------|----------|------|
| Docker | 24.x | 24.0.7 | 容器运行时 |
| Docker Compose | 2.x | 2.21.0 | 容器编排 |
| MySQL | 8.0.x | 8.0.34 | 关系数据库 |
| Redis | 7.x | 7.2.4 | 缓存数据库 |
| Nginx | 1.24.x | 1.24.0 | 反向代理 |
| MinIO | 最新 | latest | 对象存储 |
| JDK | 17 | 17.0.2 | Java运行环境 |

#### 网络环境

| 项目 | 说明 |
|------|------|
| 服务器IP | 公网IP或内网IP |
| 域名 | ems.company.com（示例） |
| SSL证书 | Let's Encrypt免费证书 |
| 开放端口 | 80, 443, 3306(内网), 6379(内网), 9000(内网) |

---

## 2. 部署前准备

### 2.1 服务器初始化

```bash
# 更新系统包
sudo apt update && sudo apt upgrade -y

# 安装必要工具
sudo apt install -y curl wget git vim unzip

# 配置时区
sudo timedatectl set-timezone Asia/Shanghai

# 创建应用目录
sudo mkdir -p /opt/ems
sudo chown -R $USER:$USER /opt/ems
```

### 2.2 Docker环境安装

```bash
# 安装Docker
curl -fsSL https://get.docker.com | sudo sh
sudo systemctl enable docker
sudo systemctl start docker

# 验证Docker安装
docker --version

# 安装Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" \
  -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# 验证Docker Compose安装
docker-compose --version
```

### 2.3 防火墙配置

```bash
# 查看防火墙状态
sudo ufw status

# 开放必要端口
sudo ufw allow 80/tcp      # HTTP
sudo ufw allow 443/tcp     # HTTPS
sudo ufw allow 22/tcp      # SSH

# 启动防火墙
sudo ufw enable
sudo ufw reload
```

### 2.4 SSL证书配置

```bash
# 安装Certbot
sudo apt install certbot python3-certbot-nginx -y

# 申请SSL证书
sudo certbot --nginx -d ems.company.com

# 自动续期测试
sudo certbot renew --dry-run
```

---

## 3. 应用部署

### 3.1 获取部署包

```bash
# 进入应用目录
cd /opt/ems

# 克隆代码仓库（或上传部署包）
git clone https://github.com/your-org/enterprise-employee-system.git .

# 切换到稳定版本
git checkout tags/v1.0.0
```

### 3.2 配置环境变量

```bash
# 创建环境变量文件
cat > .env << 'EOF'
# MySQL配置
MYSQL_ROOT_PASSWORD=your_secure_password
MYSQL_DATABASE=enterprise_ems

# Redis配置
REDIS_PASSWORD=your_redis_password

# MinIO配置
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=your_minio_secret

# JWT配置
JWT_SECRET=your_jwt_secret_key_at_least_32_characters_long

# 系统配置
SYSTEM_NAME=企业员工管理系统
SYSTEM_URL=https://ems.company.com
EOF

# 设置文件权限
chmod 600 .env
```

### 3.3 配置Docker Compose

```bash
# 创建docker-compose.yml
cat > docker-compose.yml << 'EOF'
version: '3.8'

services:
  # Nginx反向代理
  nginx:
    image: nginx:1.24-alpine
    container_name: ems-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d
      - ./nginx/ssl:/etc/nginx/ssl
      - ./nginx/html:/usr/share/nginx/html
      - ./logs/nginx:/var/log/nginx
    depends_on:
      - backend
    restart: always
    networks:
      - ems-network

  # 后端应用
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: ems-backend
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - MYSQL_HOST=mysql
      - MYSQL_PORT=3306
      - MYSQL_DATABASE=${MYSQL_DATABASE}
      - MYSQL_USERNAME=root
      - MYSQL_PASSWORD=${MYSQL_ROOT_PASSWORD}
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_PASSWORD=${REDIS_PASSWORD}
      - MINIO_URL=http://minio:9000
      - MINIO_ACCESS_KEY=${MINIO_ACCESS_KEY}
      - MINIO_SECRET_KEY=${MINIO_SECRET_KEY}
      - JWT_SECRET=${JWT_SECRET}
      - SYSTEM_URL=${SYSTEM_URL}
    depends_on:
      - mysql
      - redis
      - minio
    volumes:
      - ./logs/backend:/app/logs
      - ./uploads:/app/uploads
    restart: always
    networks:
      - ems-network

  # MySQL数据库
  mysql:
    image: mysql:8.0
    container_name: ems-mysql
    ports:
      - "127.0.0.1:3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
      - MYSQL_DATABASE=${MYSQL_DATABASE}
      - MYSQL_CHARSET=utf8mb4
      - MYSQL_COLLATE=utf8mb4_unicode_ci
    volumes:
      - mysql_data:/var/lib/mysql
      - ./sql/init:/docker-entrypoint-initdb.d
    command: >
      --character-set-server=utf8mb4
      --collation-server=utf8mb4_unicode_ci
      --default-authentication-plugin=mysql_native_password
    restart: always
    networks:
      - ems-network

  # Redis缓存
  redis:
    image: redis:7-alpine
    container_name: ems-redis
    ports:
      - "127.0.0.1:6379:6379"
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD}
    volumes:
      - redis_data:/data
    restart: always
    networks:
      - ems-network

  # MinIO对象存储
  minio:
    image: minio/minio:latest
    container_name: ems-minio
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      - MINIO_ROOT_USER=${MINIO_ACCESS_KEY}
      - MINIO_ROOT_PASSWORD=${MINIO_SECRET_KEY}
    volumes:
      - minio_data:/data
    command: server /data --console-address ":9001"
    restart: always
    networks:
      - ems-network

networks:
  ems-network:
    driver: bridge

volumes:
  mysql_data:
  redis_data:
  minio_data:
EOF
```

### 3.4 创建Nginx配置

```bash
# 创建Nginx配置目录
mkdir -p nginx/conf.d nginx/ssl nginx/html logs/nginx logs/backend

# 创建主配置文件
cat > nginx/conf.d/default.conf << 'EOF'
server {
    listen 80;
    server_name ems.company.com;
    
    # HTTP重定向到HTTPS
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name ems.company.com;
    
    # SSL证书配置
    ssl_certificate /etc/nginx/ssl/fullchain.pem;
    ssl_certificate_key /etc/nginx/ssl/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    
    # 客户端上传限制
    client_max_body_size 50m;
    
    # Gzip压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript image/svg+xml;
    gzip_min_length 1000;
    
    # 静态资源缓存
    location ~* \.(?:css|js|jpg|jpeg|gif|ico|png|svg|woff|woff2|ttf|eot)$ {
        root /usr/share/nginx/html;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
    
    # API代理
    location /api/ {
        proxy_pass http://backend:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 60s;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
    }
    
    # WebSocket支持
    location /ws/ {
        proxy_pass http://backend:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
    }
    
    # 前端路由模式支持
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
    
    # 健康检查
    location /health {
        proxy_pass http://backend:8080/actuator/health;
        access_log off;
    }
}
EOF

# 复制SSL证书
sudo cp /etc/letsencrypt/live/ems.company.com/fullchain.pem nginx/ssl/
sudo cp /etc/letsencrypt/live/ems.company.com/privkey.pem nginx/ssl/
```

### 3.5 创建Dockerfile

```bash
# 创建后端Dockerfile
cat > backend/Dockerfile << 'EOF'
FROM eclipse-temurin:17-jdk-alpine AS builder
WORKDIR /app
COPY pom.xml .
RUN mvn dependency:go-offline -B
COPY src ./src
RUN mvn clean package -DskipTests -B

FROM eclipse-temurin:17-jre-alpine
WORKDIR /app
COPY --from=builder /app/target/*.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "app.jar"]
EOF

# 创建前端Dockerfile
cat > frontend/Dockerfile << 'EOF'
FROM node:18-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

FROM nginx:1.24-alpine
COPY --from=builder /app/dist /usr/share/nginx/html
EXPOSE 80
EOF
```

### 3.6 构建和启动

```bash
# 构建后端镜像
cd backend
docker build -t ems-backend:latest .
cd ..

# 构建前端镜像（如果需要单独部署）
# cd frontend
# docker build -t ems-frontend:latest .
# cd ..

# 启动所有服务
docker-compose up -d

# 查看服务状态
docker-compose ps

# 查看日志
docker-compose logs -f backend
```

---

## 4. 数据库初始化

### 4.1 数据库建表

Docker Compose启动时会自动执行初始化脚本。如需手动执行：

```bash
# 复制建表脚本到容器
docker cp sql/schema.sql ems-mysql:/tmp/

# 进入MySQL容器执行脚本
docker exec -it ems-mysql mysql -uroot -p${MYSQL_ROOT_PASSWORD} enterprise_ems -e "SOURCE /tmp/schema.sql"
```

### 4.2 初始数据导入

```bash
# 复制初始数据脚本
docker cp sql/data.sql ems-mysql:/tmp/

# 执行初始数据导入
docker exec -it ems-mysql mysql -uroot -p${MYSQL_ROOT_PASSWORD} enterprise_ems -e "SOURCE /tmp/data.sql"
```

**初始数据包含：**
- 默认管理员账号（admin/admin123）
- 基础角色和权限
- 部门结构示例
- 系统配置参数

### 4.3 MinIO存储桶创建

```bash
# 进入MinIO控制台或使用mc命令行
docker exec -it ems-minio mc alias set myminio http://localhost:9000 minioadmin your_minio_secret

# 创建存储桶
docker exec -it ems-minio mc mb myminio/enterprise-ems

# 设置公开读取权限（如果需要）
docker exec -it ems-minio mc anonymous set download myminio/enterprise-ems
```

---

## 5. 验证与测试

### 5.1 服务健康检查

```bash
# 检查Nginx
curl -I http://localhost:80

# 检查后端服务
curl http://localhost:8080/actuator/health

# 检查MySQL连接
docker exec -it ems-mysql mysql -uroot -p${MYSQL_ROOT_PASSWORD} -e "SELECT 1"

# 检查Redis连接
docker exec -it ems-redis redis-cli -a ${REDIS_PASSWORD} ping
```

### 5.2 功能验证清单

| 序号 | 验证项 | 验证方法 | 预期结果 | 状态 |
|------|--------|----------|----------|------|
| 1 | 登录功能 | 访问登录页，输入账号密码 | 登录成功进入首页 | 待验证 |
| 2 | 员工列表 | 访问员工管理页面 | 显示员工列表数据 | 待验证 |
| 3 | 新增员工 | 填写员工表单并提交 | 创建成功 | 待验证 |
| 4 | 部门管理 | 创建新部门 | 创建成功 | 待验证 |
| 5 | 考勤打卡 | 点击打卡按钮 | 打卡成功 | 待验证 |
| 6 | 请假申请 | 提交请假申请 | 提交成功 | 待验证 |
| 7 | 薪资查看 | 查看个人薪资条 | 显示薪资详情 | 待验证 |
| 8 | 权限控制 | 普通用户访问管理员接口 | 返回403无权限 | 待验证 |
| 9 | 文件上传 | 上传员工头像 | 上传成功 | 待验证 |
| 10 | 数据导出 | 导出员工Excel | 下载成功 | 待验证 |

### 5.3 冒烟测试脚本

```bash
#!/bin/bash

echo "=== 企业员工管理系统冒烟测试 ==="

BASE_URL="https://ems.company.com"
TOKEN=""

echo "1. 测试登录..."
LOGIN_RESPONSE=$(curl -s -X POST "$BASE_URL/api/v1/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}')

if echo "$LOGIN_RESPONSE" | grep -q '"code":200'; then
    echo "✅ 登录成功"
    TOKEN=$(echo "$LOGIN_RESPONSE" | grep -o '"token":"[^"]*"' | cut -d'"' -f4)
else
    echo "❌ 登录失败"
    exit 1
fi

echo "2. 测试获取用户信息..."
USER_RESPONSE=$(curl -s -X GET "$BASE_URL/api/v1/auth/current-user" \
  -H "Authorization: Bearer $TOKEN")

if echo "$USER_RESPONSE" | grep -q '"code":200'; then
    echo "✅ 获取用户信息成功"
else
    echo "❌ 获取用户信息失败"
fi

echo "3. 测试员工列表..."
EMP_RESPONSE=$(curl -s -X GET "$BASE_URL/api/v1/employees?page=1&pageSize=10" \
  -H "Authorization: Bearer $TOKEN")

if echo "$EMP_RESPONSE" | grep -q '"code":200'; then
    echo "✅ 员工列表查询成功"
else
    echo "❌ 员工列表查询失败"
fi

echo "4. 测试部门树..."
DEPT_RESPONSE=$(curl -s -X GET "$BASE_URL/api/v1/departments/tree" \
  -H "Authorization: Bearer $TOKEN")

if echo "$DEPT_RESPONSE" | grep -q '"code":200'; then
    echo "✅ 部门树查询成功"
else
    echo "❌ 部门树查询失败"
fi

echo "5. 测试数据看板..."
DASHBOARD_RESPONSE=$(curl -s -X GET "$BASE_URL/api/v1/statistics/dashboard/employee" \
  -H "Authorization: Bearer $TOKEN")

if echo "$DASHBOARD_RESPONSE" | grep -q '"code":200'; then
    echo "✅ 数据看板查询成功"
else
    echo "❌ 数据看板查询失败"
fi

echo ""
echo "=== 冒烟测试完成 ==="
```

---

## 6. 运维管理

### 6.1 日志管理

```bash
# 查看后端日志
docker-compose logs -f --tail=100 backend

# 查看Nginx访问日志
tail -f logs/nginx/access.log

# 查看Nginx错误日志
tail -f logs/nginx/error.log

# 查看MySQL错误日志
docker exec -it ems-mysql tail -f /var/log/mysql/error.log
```

### 6.2 数据备份

```bash
# 创建备份脚本
cat > /opt/ems/backup.sh << 'EOF'
#!/bin/bash

BACKUP_DIR="/opt/ems/backups"
DATE=$(date +%Y%m%d_%H%M%S)

# 创建备份目录
mkdir -p $BACKUP_DIR

# MySQL备份
echo "备份MySQL数据库..."
docker exec ems-mysql mysqldump -uroot -p${MYSQL_ROOT_PASSWORD} enterprise_ems > $BACKUP_DIR/mysql_$DATE.sql

# Redis备份
echo "备份Redis数据..."
docker exec ems-redis redis-cli -a ${REDIS_PASSWORD} BGSAVE
sleep 5
docker cp ems-redis:/data/dump.rdb $BACKUP_DIR/redis_$DATE.rdb

# MinIO备份
echo "备份MinIO数据..."
docker run --rm -v minio_data:/data -v $BACKUP_DIR:/backup alpine tar czf /backup/minio_$DATE.tar.gz -C /data .

# 删除30天前的备份
find $BACKUP_DIR -name "*.sql" -mtime +30 -delete
find $BACKUP_DIR -name "*.rdb" -mtime +30 -delete
find $BACKUP_DIR -name "*.tar.gz" -mtime +30 -delete

echo "备份完成: $DATE"
EOF

# 添加定时任务（每天凌晨3点）
crontab -e
0 3 * * * /opt/ems/backup.sh >> /opt/ems/logs/backup.log 2>&1
```

### 6.3 版本更新

```bash
# 1. 拉取新版本
cd /opt/ems
git fetch origin
git checkout tags/v1.1.0

# 2. 更新配置（如有变化）
# 对比env文件差异，更新环境变量

# 3. 重新构建并启动
docker-compose build --no-cache backend
docker-compose up -d

# 4. 数据库迁移（如有）
docker exec -it ems-mysql mysql -uroot -p${MYSQL_ROOT_PASSWORD} enterprise_ems -e "SOURCE /opt/ems/sql/migration/v1.1.0.sql"

# 5. 验证新版本
curl http://localhost:8080/actuator/info

# 6. 回滚方案（如更新失败）
git checkout tags/v1.0.0
docker-compose build --no-cache backend
docker-compose up -d
```

### 6.4 常见问题处理

| 问题编号 | 问题描述 | 原因分析 | 解决方案 |
|----------|----------|----------|----------|
| Q-001 | 无法登录 | 数据库连接失败 | 检查MySQL服务状态，查看连接配置 |
| Q-002 | 页面加载慢 | Nginx配置问题 | 检查gzip配置，增加缓存 |
| Q-003 | 502 Bad Gateway | 后端服务未启动 | 重启后端服务，查看启动日志 |
| Q-004 | 数据库连接超时 | 网络或配置问题 | 检查MySQL端口开放，验证连接参数 |
| Q-005 | 文件上传失败 | MinIO服务异常 | 检查MinIO服务状态和存储桶配置 |
| Q-006 | Token失效太快 | JWT配置问题 | 调整jwt.access-token.expiration参数 |
| Q-007 | 中文显示乱码 | 字符集配置问题 | 确保数据库和连接使用utf8mb4 |
| Q-008 | 定时任务不执行 | 应用未开启调度 | 确认@EnableScheduling注解 |

### 6.5 紧急联系方式

| 角色 | 联系人 | 联系方式 | 职责 |
|------|--------|----------|------|
| 系统管理员 | 张三 | admin@company.com | 系统运维支持 |
| 数据库管理员 | 张三 | dba@company.com | 数据库问题处理 |
| 网络管理员 | （预留） | - | 网络问题处理 |

---

## 附录

### 附录A：端口清单

| 服务 | 端口 | 协议 | 是否公网访问 | 说明 |
|------|------|------|--------------|------|
| Nginx HTTP | 80 | HTTP | 是 | Web服务入口 |
| Nginx HTTPS | 443 | HTTPS | 是 | Web服务入口（推荐） |
| Spring Boot | 8080 | HTTP | 否（容器内部） | 后端应用服务 |
| MySQL | 3306 | TCP | 否（127.0.0.1绑定） | 数据库服务 |
| Redis | 6379 | TCP | 否（127.0.0.1绑定） | 缓存服务 |
| MinIO API | 9000 | HTTP | 是 | 对象存储API |
| MinIO Console | 9001 | HTTP | 是 | 对象存储控制台 |
| SSH | 22 | TCP | 是 | 远程管理 |

### 附录B：常用命令速查

```bash
# Docker Compose常用命令
docker-compose up -d                    # 启动所有服务
docker-compose down                     # 停止所有服务
docker-compose restart                  # 重启所有服务
docker-compose logs -f                  # 查看所有日志
docker-compose ps                       # 查看服务状态
docker-compose exec backend bash        # 进入后端容器
docker-compose exec mysql mysql -uroot -p  # 进入MySQL客户端

# Docker常用命令
docker ps                               # 查看运行中的容器
docker logs -f container_id              # 查看容器日志
docker exec -it container_id bash       # 进入容器
docker stats                            # 查看资源使用情况
docker system prune                     # 清理无用镜像和容器

# MySQL常用命令
mysql -uroot -p                         # 连接数据库
SHOW DATABASES;                         # 查看数据库列表
USE enterprise_ems;                     # 切换数据库
SHOW TABLES;                            # 查看表列表
DESC sys_user;                          # 查看表结构
SELECT * FROM sys_user LIMIT 10;        # 查询数据
SELECT COUNT(*) FROM em_employee;       # 统计员工数量

# Redis常用命令
redis-cli -a password                   # 连接Redis
PING                                    # 测试连接
KEYS *                                  # 查看所有key
GET key_name                            # 获取key值
DEL key_name                            # 删除key
FLUSHALL                                # 清空所有数据（谨慎使用）

# Nginx常用命令
nginx -t                                # 测试配置
nginx -s reload                         # 重新加载配置
nginx -s stop                           # 停止Nginx
tail -f /var/log/nginx/access.log       # 查看访问日志
tail -f /var/log/nginx/error.log        # 查看错误日志
```

### 附录C：服务器资源监控

建议配置监控告警，当以下指标超过阈值时通知管理员：

| 指标 | 告警阈值 | 检查命令 |
|------|----------|----------|
| CPU使用率 | > 80% | `top -bn1 | grep "Cpu(s)"` |
| 内存使用率 | > 85% | `free -m` |
| 磁盘使用率 | > 90% | `df -h` |
| Docker容器状态 | 容器重启 | `docker ps -a --filter "status=restarting"` |
| MySQL连接数 | > 500 | `SHOW STATUS LIKE 'Threads_connected'` |
| Nginx活动连接 | > 5000 | `nginx -s stub_status` |

---

**文档版本历史：**

| 版本 | 日期 | 修改人 | 修改内容 |
|------|------|--------|----------|
| V1.0 | 2026-08-25 | 张三（单人） | 初始版本，创建部署记录文档 |
