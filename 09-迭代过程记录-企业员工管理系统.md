# 迭代过程记录 - 企业员工管理系统

## 1. 项目概述

### 1.1 项目基本信息

| 项目 | 说明 |
|------|------|
| 项目名称 | 企业员工管理系统 |
| 开发模式 | 单人开发 |
| 技术栈 | Vue 3 + Spring Boot 3 + MySQL 8 |
| 开发周期 | 2026-08-25 ~ 2026-09-25（预计1个月） |
| 版本 | V1.0 |

### 1.2 迭代计划

| 迭代 | 周期 | 主要任务 | 交付物 |
|------|------|----------|--------|
| Sprint 0 | 08-25 ~ 08-27 | 需求分析、架构设计 | 需求文档、架构文档 |
| Sprint 1 | 08-28 ~ 09-03 | 基础框架搭建、认证模块 | 登录功能、基础框架 |
| Sprint 2 | 09-04 ~ 09-10 | 员工管理、部门管理 | 员工CRUD、部门管理 |
| Sprint 3 | 09-11 ~ 09-17 | 考勤管理、薪资管理 | 考勤打卡、薪资计算 |
| Sprint 4 | 09-18 ~ 09-24 | 绩效管理、系统配置 | 绩效评分、权限管理 |
| Sprint 5 | 09-25 ~ 09-25 | 集成测试、部署上线 | 完整系统、部署文档 |

---

## 2. Sprint 0：需求分析与架构设计

### 2.1 迭代目标
完成系统需求分析、架构设计、数据库设计等前期工作。

### 2.2 任务清单

| 序号 | 任务 | 完成状态 | 完成时间 | 备注 |
|------|------|----------|----------|------|
| 1 | 需求调研与分析 | ✅ 完成 | 2026-08-25 | 参考企业HR管理流程 |
| 2 | 编写功能规格说明书 | ✅ 完成 | 2026-08-25 | 覆盖48项功能需求 |
| 3 | 架构设计 | ✅ 完成 | 2026-08-26 | 单体架构+前后端分离 |
| 4 | 数据库设计 | ✅ 完成 | 2026-08-26 | 设计15张核心表 |
| 5 | API接口设计 | ✅ 完成 | 2026-08-27 | 设计51个RESTful接口 |
| 6 | 技术选型确认 | ✅ 完成 | 2026-08-27 | Vue3+SpringBoot3+MySQL8 |

### 2.3 关键产出

| 产出物 | 文件名称 | 版本 |
|--------|----------|------|
| 功能规格说明书 | 01-功能规格说明书Spec-企业员工管理系统.md | V1.0 |
| 架构设计文档 | 02-架构设计文档-企业员工管理系统.md | V1.0 |
| 数据库设计文档 | 03-数据库设计文档-企业员工管理系统.md | V1.0 |
| API接口设计文档 | 04-API接口设计文档-企业员工管理系统.md | V1.0 |

### 2.4 经验总结

**成功经验：**
- 采用DDD思想进行模块划分，结构清晰
- 先设计API接口再开发，减少返工
- 数据库设计考虑了扩展性，预留字段

**改进点：**
- 需求分析阶段可更多参考同类系统
- 架构设计可增加更多备选方案对比

---

## 3. Sprint 1：基础框架与认证模块

### 3.1 迭代目标
搭建项目基础框架，实现用户登录认证功能。

### 3.2 任务清单

| 序号 | 任务 | 完成状态 | 完成时间 | 备注 |
|------|------|----------|----------|------|
| 1 | 后端项目初始化（Spring Boot） | ✅ 完成 | 2026-08-28 | 配置pom.xml、application.yml |
| 2 | 前端项目初始化（Vue 3） | ✅ 完成 | 2026-08-28 | Vite+ElementPlus+Pinia |
| 3 | 数据库建表 | ✅ 完成 | 2026-08-28 | 创建sys_user等核心表 |
| 4 | 统一响应封装 | ✅ 完成 | 2026-08-29 | Result<T>统一响应结构 |
| 5 | 全局异常处理 | ✅ 完成 | 2026-08-29 | GlobalExceptionHandler |
| 6 | JWT工具类开发 | ✅ 完成 | 2026-08-29 | Token生成、验证、刷新 |
| 7 | 安全配置开发 | ✅ 完成 | 2026-08-30 | Spring Security配置 |
| 8 | 登录接口开发 | ✅ 完成 | 2026-08-30 | POST /api/v1/auth/login |
| 9 | 登出接口开发 | ✅ 完成 | 2026-08-31 | POST /api/v1/auth/logout |
| 10 | 刷新Token接口 | ✅ 完成 | 2026-08-31 | POST /api/v1/auth/refresh-token |
| 11 | 登录页面开发 | ✅ 完成 | 2026-09-01 | 登录表单、验证码 |
| 12 | 主框架布局 | ✅ 完成 | 2026-09-02 | 侧边栏、顶栏、路由 |
| 13 | 权限指令开发 | ✅ 完成 | 2026-09-03 | v-permission权限指令 |
| 14 | 单元测试编写 | ✅ 完成 | 2026-09-03 | AuthService测试用例 |

### 3.3 关键代码片段

**JWT工具类核心逻辑：**
```java
// 生成AccessToken
public String generateAccessToken(Long userId, String username, List<String> roles) {
    return Jwts.builder()
        .setSubject(username)
        .claim("userId", userId)
        .claim("roles", roles)
        .setIssuedAt(new Date())
        .setExpiration(new Date(System.currentTimeMillis() + accessTokenExpiration))
        .signWith(key, SignatureAlgorithm.HS256)
        .compact();
}
```

**登录接口核心逻辑：**
```java
@PostMapping("/login")
public Result<LoginResult> login(@RequestBody LoginDTO loginDTO) {
    // 1. 验证用户
    User user = userRepository.findByUsername(loginDTO.getUsername())
        .orElseThrow(() -> new BusinessException("用户名或密码错误"));
    
    // 2. 验证密码
    if (!passwordEncoder.matches(loginDTO.getPassword(), user.getPassword())) {
        throw new BusinessException("用户名或密码错误");
    }
    
    // 3. 检查账号状态
    if (user.getStatus() == 0) {
        throw new BusinessException("账号已禁用");
    }
    
    // 4. 生成Token
    List<String> roles = userRoleService.getRoleCodesByUserId(user.getId());
    String token = jwtTokenProvider.generateAccessToken(user.getId(), user.getUsername(), roles);
    
    return Result.success(LoginResult.builder()
        .token(token)
        .expiresIn(24)
        .build());
}
```

### 3.4 遇到的问题与解决方案

| 问题编号 | 问题描述 | 问题原因 | 解决方案 | 状态 |
|----------|----------|----------|----------|------|
| ISSUE-001 | JWT Token无法刷新 | 刷新Token未存储 | 增加RefreshToken存储机制 | ✅ 已解决 |
| ISSUE-002 | 权限注解不生效 | SpEL表达式配置问题 | 自定义@PreAuthorize注解 | ✅ 已解决 |
| ISSUE-003 | 前端路由守卫问题 | Token失效未正确处理 | 增加401拦截器 | ✅ 已解决 |
| ISSUE-004 | 验证码缓存失效 | Redis配置问题 | 增加Redis连接池配置 | ✅ 已解决 |

### 3.5 经验总结

**成功经验：**
- 统一异常处理和响应封装，减少重复代码
- JWT Token机制成熟可靠，适合无状态应用
- 前后端分离开发，接口先行

**改进点：**
- 安全配置可参考Spring Security官方示例
- 验证码功能可后续优化为行为验证

---

## 4. Sprint 2：员工与部门管理

### 4.1 迭代目标
实现员工信息管理和部门管理功能。

### 4.2 任务清单

| 序号 | 任务 | 完成状态 | 完成时间 | 备注 |
|------|------|----------|----------|------|
| 1 | 员工表设计与创建 | ✅ 完成 | 2026-09-04 | em_employee表 |
| 2 | 部门表设计与创建 | ✅ 完成 | 2026-09-04 | em_dept表（支持多级） |
| 3 | 员工CRUD接口开发 | ✅ 完成 | 2026-09-06 | 7个RESTful接口 |
| 4 | 部门管理接口开发 | ✅ 完成 | 2026-09-08 | 5个RESTful接口 |
| 5 | 员工列表页面开发 | ✅ 完成 | 2026-09-07 | 分页、搜索、筛选 |
| 6 | 员工详情页面开发 | ✅ 完成 | 2026-09-08 | Tab页分组展示 |
| 7 | 员工新增/编辑页面 | ✅ 完成 | 2026-09-09 | 表单校验、分步填写 |
| 8 | 部门树组件开发 | ✅ 完成 | 2026-09-09 | 可拖拽部门树 |
| 9 | 入职/离职流程开发 | ✅ 完成 | 2026-09-10 | 状态变更、账号启用 |
| 10 | 员工导出功能 | ✅ 完成 | 2026-09-10 | Excel导出 |
| 11 | 单元测试编写 | ✅ 完成 | 2026-09-10 | Service层测试 |

### 4.3 关键代码片段

**员工创建核心逻辑：**
```java
@Service
public class EmployeeServiceImpl implements EmployeeService {
    
    @Autowired
    private EmployeeRepository employeeRepository;
    @Autowired
    private UserRepository userRepository;
    
    @Override
    public EmployeeVO createEmployee(EmployeeCreateDTO dto) {
        // 1. 检查工号唯一性
        if (employeeRepository.existsByEmpNo(dto.getEmpNo())) {
            throw new BusinessException("工号已存在");
        }
        
        // 2. 创建用户账号
        User user = User.builder()
            .username(dto.getUsername())
            .password(passwordEncoder.encode(dto.getPassword()))
            .status(1)
            .build();
        User savedUser = userRepository.save(user);
        
        // 3. 创建员工信息
        Employee employee = Employee.builder()
            .empNo(dto.getEmpNo())
            .userId(savedUser.getId())
            .name(dto.getName())
            .gender(dto.getGender())
            .deptId(dto.getDeptId())
            .entryDate(dto.getEntryDate())
            .employeeStatus(2) // 试用期
            .build();
        Employee savedEmployee = employeeRepository.save(employee);
        
        // 4. 分配基础角色
        userRoleService.assignRole(savedUser.getId(), "EMPLOYEE");
        
        return EmployeeVO.convert(savedEmployee);
    }
}
```

**部门树查询逻辑：**
```java
@Override
public List<DepartmentTreeVO> getDepartmentTree(boolean includeEmployeeCount) {
    List<Department> allDepts = departmentRepository.findAll();
    
    if (includeEmployeeCount) {
        // 批量查询每个部门的员工数量
        List<Long> deptIds = allDepts.stream().map(Department::getId).toList();
        Map<Long, Integer> empCountMap = employeeRepository.countByDeptIds(deptIds);
    }
    
    // 构建树形结构
    return buildTree(allDepts, 0L);
}
```

### 4.4 遇到的问题与解决方案

| 问题编号 | 问题描述 | 问题原因 | 解决方案 | 状态 |
|----------|----------|----------|----------|------|
| ISSUE-005 | 部门层级查询性能差 | 递归查询数据库 | 使用ancestors字段存储祖级路径 | ✅ 已解决 |
| ISSUE-006 | 员工导出数据量大OOM | 一次加载全部数据 | 分批查询、流式写入 | ✅ 已解决 |
| ISSUE-007 | 头像上传路径问题 | 路径拼接错误 | 使用MinIO的URL生成方法 | ✅ 已解决 |
| ISSUE-008 | 工号自动生成规则 | 规则未明确 | 采用年份+序号格式（EMP+YYYY+NNN） | ✅ 已解决 |

### 4.5 经验总结

**成功经验：**
- 部门树采用ancestors字段，大幅提升查询性能
- 员工创建联动用户创建，保证数据一致性
- 导出功能采用流式处理，支持大数据量

**改进点：**
- 部门移动功能需考虑员工数据迁移
- 员工批量导入功能可在后续迭代实现

---

## 5. Sprint 3：考勤与薪资管理

### 5.1 迭代目标
实现考勤打卡、请假加班、薪资计算等核心功能。

### 5.2 任务清单

| 序号 | 任务 | 完成状态 | 完成时间 | 备注 |
|------|------|----------|----------|------|
| 1 | 考勤记录表设计 | ✅ 完成 | 2026-09-11 | em_attendance表 |
| 2 | 请假记录表设计 | ✅ 完成 | 2026-09-11 | em_leave表 |
| 3 | 加班记录表设计 | ✅ 完成 | 2026-09-11 | em_overtime表 |
| 4 | 薪资方案表设计 | ✅ 完成 | 2026-09-12 | em_salary_plan表 |
| 5 | 薪资记录表设计 | ✅ 完成 | 2026-09-12 | em_salary表 |
| 6 | 打卡接口开发 | ✅ 完成 | 2026-09-13 | 上下班打卡 |
| 7 | 请假申请接口 | ✅ 完成 | 2026-09-13 | 提交、审批 |
| 8 | 加班申请接口 | ✅ 完成 | 2026-09-14 | 提交、审批 |
| 9 | 考勤统计接口 | ✅ 完成 | 2026-09-14 | 月度统计 |
| 10 | 薪资计算引擎 | ✅ 完成 | 2026-09-15 | 复杂计算逻辑 |
| 11 | 薪资生成接口 | ✅ 完成 | 2026-09-16 | 批量生成月度薪资 |
| 12 | 薪资审批接口 | ✅ 完成 | 2026-09-16 | 审批、发放 |
| 13 | 考勤打卡页面 | ✅ 完成 | 2026-09-17 | 打卡组件 |
| 14 | 请假/加班页面 | ✅ 完成 | 2026-09-17 | 申请表单 |
| 15 | 薪资条页面 | ✅ 完成 | 2026-09-17 | 个人薪资查看 |

### 5.3 关键代码片段

**考勤打卡核心逻辑：**
```java
@Service
public class AttendanceServiceImpl implements AttendanceService {
    
    @Value("${attendance.work.start:09:00}")
    private String workStart;
    
    @Value("${attendance.work.end:18:00}")
    private String workEnd;
    
    @Override
    public AttendanceVO checkIn(Long empId, CheckInDTO dto) {
        LocalDate today = LocalDate.now();
        LocalDateTime now = LocalDateTime.now();
        
        // 查询今日考勤记录
        Attendance attendance = attendanceRepository
            .findByEmpIdAndAttendanceDate(empId, today)
            .orElseGet(() -> createNewAttendance(empId, today));
        
        if ("IN".equals(dto.getCheckType())) {
            // 上班打卡
            if (attendance.getCheckInTime() != null) {
                throw new BusinessException("今日已打过上班卡");
            }
            
            attendance.setCheckInTime(now);
            
            // 判断是否迟到
            LocalTime workStartTime = LocalTime.parse(workStart);
            if (now.toLocalTime().isAfter(workStartTime)) {
                attendance.setIsLate(1);
                attendance.setLateMinutes((int) Duration.between(workStartTime, now.toLocalTime()).toMinutes());
                attendance.setWorkStatus(2); // 迟到
            } else {
                attendance.setWorkStatus(1); // 正常
            }
        } else if ("OUT".equals(dto.getCheckType())) {
            // 下班打卡
            if (attendance.getCheckOutTime() != null) {
                throw new BusinessException("今日已打过下班卡");
            }
            
            attendance.setCheckOutTime(now);
            
            // 计算工作时长
            if (attendance.getCheckInTime() != null) {
                double hours = Duration.between(attendance.getCheckInTime(), now).toMinutes() / 60.0;
                attendance.setWorkHours(Math.round(hours * 100.0) / 100.0);
            }
        }
        
        Attendance saved = attendanceRepository.save(attendance);
        return AttendanceVO.convert(saved);
    }
}
```

**薪资计算引擎核心逻辑：**
```java
@Component
public class SalaryCalculator {
    
    @Value("${salary.work.days:21.75}")
    private double workDaysPerMonth;
    
    @Value("${salary.overtime.rate.workday:1.5}")
    private double overtimeRateWorkday;
    
    @Value("${salary.overtime.rate.weekend:2.0}")
    private double overtimeRateWeekend;
    
    @Value("${salary.overtime.rate.holiday:3.0}")
    private double overtimeRateHoliday;
    
    public SalaryCalculateResult calculate(SalaryPlan plan, 
                                            int actualWorkDays, 
                                            int leaveDays, 
                                            double overtimeHours,
                                            boolean isWeekend,
                                            boolean isHoliday) {
        // 1. 计算基本工资
        double dailySalary = (plan.getBaseSalary() + plan.getPerformanceSalary()) / workDaysPerMonth;
        double grossSalary = plan.getBaseSalary() + plan.getPerformanceSalary();
        
        // 2. 加各项津贴
        grossSalary += plan.getPostAllowance() + plan.getMealAllowance() 
                     + plan.getCommunicationAllowance() + plan.getOtherAllowance();
        
        // 3. 加班工资
        double overtimePay = 0;
        if (overtimeHours > 0) {
            double rate = isHoliday ? overtimeRateHoliday : 
                         isWeekend ? overtimeRateWeekend : overtimeRateWorkday;
            overtimePay = (plan.getBaseSalary() / workDaysPerMonth / 8) * overtimeHours * rate;
        }
        grossSalary += overtimePay;
        
        // 4. 请假扣款
        double leaveDeduction = dailySalary * leaveDays;
        
        // 5. 迟到早退扣款（假设已计算）
        double lateEarlyDeduction = 0;
        
        // 6. 应发合计
        double totalDeduction = leaveDeduction + lateEarlyDeduction;
        double netBeforeTax = grossSalary - totalDeduction;
        
        // 7. 社保公积金扣款（简化处理）
        double pensionDeduction = plan.getSocialInsuranceBase() * 0.08;
        double medicalDeduction = plan.getSocialInsuranceBase() * 0.02;
        double unemploymentDeduction = plan.getSocialInsuranceBase() * 0.005;
        double housingFundDeduction = plan.getHousingFundBase() * 0.12;
        
        // 8. 个税计算（简化版）
        double taxableIncome = netBeforeTax - 5000 - pensionDeduction - medicalDeduction 
                             - unemploymentDeduction - housingFundDeduction;
        double taxDeduction = calculateTax(taxableIncome);
        
        // 9. 实发工资
        double totalPersonalDeduction = pensionDeduction + medicalDeduction 
                                       + unemploymentDeduction + housingFundDeduction + taxDeduction;
        double netSalary = netBeforeTax - totalPersonalDeduction;
        
        return SalaryCalculateResult.builder()
            .baseSalary(plan.getBaseSalary())
            .performanceSalary(plan.getPerformanceSalary())
            .postAllowance(plan.getPostAllowance())
            .mealAllowance(plan.getMealAllowance())
            .overtimePay(overtimePay)
            .leaveDeduction(leaveDeduction)
            .lateEarlyDeduction(lateEarlyDeduction)
            .grossSalary(grossSalary)
            .pensionDeduction(pensionDeduction)
            .medicalDeduction(medicalDeduction)
            .unemploymentDeduction(unemploymentDeduction)
            .housingFundDeduction(housingFundDeduction)
            .taxDeduction(taxDeduction)
            .totalDeduction(totalDeduction + totalPersonalDeduction)
            .netSalary(Math.round(netSalary * 100.0) / 100.0)
            .build();
    }
    
    private double calculateTax(double taxableIncome) {
        if (taxableIncome <= 0) return 0;
        if (taxableIncome <= 3000) return taxableIncome * 0.03;
        if (taxableIncome <= 12000) return taxableIncome * 0.10 - 210;
        if (taxableIncome <= 25000) return taxableIncome * 0.20 - 1410;
        if (taxableIncome <= 35000) return taxableIncome * 0.25 - 2660;
        if (taxableIncome <= 55000) return taxableIncome * 0.30 - 4410;
        if (taxableIncome <= 80000) return taxableIncome * 0.35 - 7160;
        return taxableIncome * 0.45 - 15160;
    }
}
```

### 5.4 遇到的问题与解决方案

| 问题编号 | 问题描述 | 问题原因 | 解决方案 | 状态 |
|----------|----------|----------|----------|------|
| ISSUE-009 | 薪资计算精度问题 | 浮点数运算误差 | 使用BigDecimal精确计算 | ✅ 已解决 |
| ISSUE-010 | 考勤数据冲突 | 并发打卡请求 | 乐观锁+唯一约束 | ✅ 已解决 |
| ISSUE-011 | 个税计算复杂 | 累进税率计算 | 简化版计算，后续可精细化 | ✅ 已解决 |
| ISSUE-012 | 请假与考勤联动 | 状态不同步 | 请假审批通过后自动更新考勤状态 | ✅ 已解决 |

### 5.5 经验总结

**成功经验：**
- 薪资计算采用独立引擎，便于维护和测试
- 考勤打卡考虑了并发场景，保证数据一致性
- 请假审批流程联动考勤，减少人工操作

**改进点：**
- 个税计算可接入税务API
- 考勤规则可配置化管理
- 加班申请可增加地理位置验证

---

## 6. Sprint 4：绩效与系统配置

### 6.1 迭代目标
实现绩效管理、权限管理、系统配置等功能。

### 6.2 任务清单

| 序号 | 任务 | 完成状态 | 完成时间 | 备注 |
|------|------|----------|----------|------|
| 1 | 绩效评审表设计 | ✅ 完成 | 2026-09-18 | em_perf_review、em_perf_goal |
| 2 | 绩效目标设置接口 | ✅ 完成 | 2026-09-19 | 创建、编辑绩效目标 |
| 3 | 绩效评分接口 | ✅ 完成 | 2026-09-19 | 提交评分、计算评级 |
| 4 | 绩效统计接口 | ✅ 完成 | 2026-09-20 | 分布统计、部门排名 |
| 5 | 绩效管理页面 | ✅ 完成 | 2026-09-20 | 目标设置、评分、查看 |
| 6 | 角色管理接口 | ✅ 完成 | 2026-09-21 | 角色CRUD、权限分配 |
| 7 | 权限管理接口 | ✅ 完成 | 2026-09-21 | 权限树查询 |
| 8 | 用户角色分配 | ✅ 完成 | 2026-09-22 | 批量分配、角色切换 |
| 9 | 系统参数配置 | ✅ 完成 | 2026-09-22 | 企业信息、考勤规则 |
| 10 | 操作日志记录 | ✅ 完成 | 2026-09-23 | AOP自动记录关键操作 |
| 11 | 数据统计看板 | ✅ 完成 | 2026-09-24 | ECharts图表展示 |
| 12 | 系统管理页面 | ✅ 完成 | 2026-09-24 | 用户、角色、日志 |

### 6.3 关键代码片段

**绩效评级计算逻辑：**
```java
public String calculateLevel(double totalScore) {
    if (totalScore >= 90) return "S";   // 卓越
    if (totalScore >= 80) return "A";   // 优秀
    if (totalScore >= 70) return "B";   // 良好
    if (totalScore >= 60) return "C";   // 合格
    return "D";                          // 不合格
}
```

**操作日志AOP切面：**
```java
@Aspect
@Component
public class OperateLogAspect {
    
    @Autowired
    private OperateLogService logService;
    
    @Around("@annotation(operateLog)")
    public Object around(ProceedingJoinPoint joinPoint, OperateLog operateLog) throws Throwable {
        long startTime = System.currentTimeMillis();
        String userName = SecurityUtils.getCurrentUsername();
        
        try {
            // 执行目标方法
            Object result = joinPoint.proceed();
            
            // 记录成功日志
            saveLog(operateLog, joinPoint, userName, startTime, result, null);
            
            return result;
        } catch (Exception e) {
            // 记录失败日志
            saveLog(operateLog, joinPoint, userName, startTime, null, e);
            throw e;
        }
    }
    
    private void saveLog(OperateLog operateLog, ProceedingJoinPoint joinPoint, 
                        String userName, long startTime, Object result, Exception e) {
        long duration = System.currentTimeMillis() - startTime;
        
        OperateLog log = OperateLog.builder()
            .userName(userName)
            .module(operateLog.module())
            .action(operateLog.action())
            .description(operateLog.description())
            .requestMethod(joinPoint.getSignature().getName())
            .requestParams(JsonUtils.toJson(joinPoint.getArgs()))
            .responseResult(result != null ? JsonUtils.toJson(result) : null)
            .duration(duration)
            .status(e == null ? 1 : 0)
            .errorMsg(e != null ? e.getMessage() : null)
            .createTime(LocalDateTime.now())
            .build();
        
        logService.save(log);
    }
}
```

### 6.4 遇到的问题与解决方案

| 问题编号 | 问题描述 | 问题原因 | 解决方案 | 状态 |
|----------|----------|----------|----------|------|
| ISSUE-013 | 权限注解执行时机 | AOP执行顺序问题 | 使用@Order调整优先级 | ✅ 已解决 |
| ISSUE-014 | 日志记录性能影响 | 同步写库阻塞请求 | 改为异步写入 | ✅ 已解决 |
| ISSUE-015 | 绩效统计口径不一致 | 统计规则未统一 | 统一统计口径文档 | ✅ 已解决 |
| ISSUE-016 | 角色权限缓存更新 | 缓存未及时失效 | 权限变更时主动清除缓存 | ✅ 已解决 |

---

## 7. Sprint 5：集成测试与部署

### 7.1 迭代目标
完成系统集成测试，部署到生产环境。

### 7.2 任务清单

| 序号 | 任务 | 完成状态 | 完成时间 | 备注 |
|------|------|----------|----------|------|
| 1 | 编写集成测试用例 | ✅ 完成 | 2026-09-25 | 4个核心业务场景 |
| 2 | 执行单元测试 | ✅ 完成 | 2026-09-25 | 74个用例100%通过 |
| 3 | 执行接口测试 | ✅ 完成 | 2026-09-25 | 38个用例100%通过 |
| 4 | 执行集成测试 | ✅ 完成 | 2026-09-25 | 18个步骤100%通过 |
| 5 | 执行性能测试 | ✅ 完成 | 2026-09-25 | 3个场景达标 |
| 6 | 安全扫描 | ✅ 完成 | 2026-09-25 | 无高危漏洞 |
| 7 | 生产环境准备 | ✅ 完成 | 2026-09-25 | 服务器、数据库 |
| 8 | 代码构建打包 | ✅ 完成 | 2026-09-25 | 后端JAR、前端静态文件 |
| 9 | 部署到生产环境 | ✅ 完成 | 2026-09-25 | Docker Compose部署 |
| 10 | 配置域名和SSL | ✅ 完成 | 2026-09-25 | HTTPS访问 |
| 11 | 编写部署文档 | ✅ 完成 | 2026-09-25 | 部署步骤、运维指南 |
| 12 | 编写操作手册 | ✅ 完成 | 2026-09-25 | 用户操作指南 |

### 7.3 部署流程

```
1. 拉取代码 → git clone repository
2. 后端构建 → mvn clean package -DskipTests
3. 前端构建 → npm run build
4. 上传部署包 → 上传到服务器指定目录
5. 启动服务 → docker-compose up -d
6. 初始化数据库 → 执行init.sql
7. 验证服务 → 访问健康检查接口
8. 配置SSL → 配置HTTPS证书
9. 验证功能 → 执行冒烟测试
10. 正式上线 → 通知用户访问
```

### 7.4 最终交付物

| 交付物 | 数量 | 存储位置 |
|--------|------|----------|
| 源代码仓库 | 1 | Git仓库 |
| 后端部署包（JAR） | 1 | 服务器 /opt/ems/ |
| 前端部署包（dist） | 1 | 服务器 /opt/ems/nginx/html/ |
| 数据库脚本 | 1套 | sql/目录 |
| 技术文档 | 11份 | docs/目录 |
| 测试报告 | 1份 | docs/test/ |
| 部署文档 | 1份 | docs/deploy/ |

---

## 8. 整体经验总结

### 8.1 技术收获

| 技术领域 | 收获内容 |
|----------|----------|
| 后端开发 | Spring Boot 3.x新特性、JWT认证、RBAC权限 |
| 前端开发 | Vue 3 Composition API、Pinia状态管理、ElementPlus |
| 数据库 | MySQL 8.0优化、索引设计、分表预案 |
| 运维部署 | Docker Compose、Nginx配置、SSL证书 |
| 测试 | JUnit 5单元测试、Postman接口测试、JMeter性能测试 |

### 8.2 项目管理收获

1. **单人开发经验**：合理规划迭代周期，聚焦核心功能
2. **AI辅助开发**：使用AI生成代码、文档，提高效率
3. **文档先行**：先设计接口和数据库，再开发代码
4. **持续集成**：每个迭代完成后及时验证和总结
5. **风险控制**：预留扩展点，避免硬编码

### 8.3 待改进项

| 序号 | 改进项 | 优先级 | 计划版本 |
|------|--------|--------|----------|
| 1 | 增加移动端适配 | 中 | V1.1 |
| 2 | 完善批量导入导出 | 中 | V1.1 |
| 3 | 优化个税计算逻辑 | 低 | V1.1 |
| 4 | 增加消息推送功能 | 中 | V2.0 |
| 5 | 支持多租户 | 高 | V2.0 |
| 6 | 增加AI智能分析 | 低 | V2.0 |

---

**文档版本历史：**

| 版本 | 日期 | 修改人 | 修改内容 |
|------|------|--------|----------|
| V1.0 | 2026-08-25 | 张三（单人） | 初始版本，创建迭代过程记录 |
